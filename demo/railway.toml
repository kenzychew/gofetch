# Railway only reads railway.toml/railway.json at the repo root, never in
# subdirectories -- see /railway.toml for the config Railway actually uses.
# This file is kept only for the historical context in its comments; it is
# not consulted by Railway.
